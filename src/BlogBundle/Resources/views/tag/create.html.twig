{% extends '@Blog/Layout/layout.html.twig' %}

{% block title %} {{ 'Create'|trans }} {% endblock %}

{% block body %}

    <div class="container">
        <section class="panel">
            <div class="panel-body">
               
                <div class="row">
                    <div class="col-md-6 offset-md-6">
                        <h3>Create blog tag</h3>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6 offset-md-6">
                        {{ form_start(form,{heading: 'New', 'attr': {'novalidate': 'novalidate'} }) }}
                        {{ form_row(form.name) }}
                        {{ form_row(form.create) }}
                        {{ form_rest(form, {offsetClass: ' '}) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </section>
    </div>

{% endblock %} 
