{% extends '@Blog/Layout/layout.html.twig' %}

{% block title %} {{ 'Blog'|trans }} {% endblock %}

{% block body %}

    <div class="container">

        <section class="panel">            
            <div class="panel-body">

                <div class="row ">
                    <div class="col-md-6 text-center">
                        <h2>{{ 'Blog Posts' }}</h2>
                    </div>
                </div>
                <div class="row ">
                    {% for post in pagination %}
                        <div class="col-md-6">
                            <h3> <a href="{{ path('frontend_show_post',{'url': post.url}) }}">{{ post.title }}</a></h3>                                       
                            <strong>  {{ post.date|date('d-m-Y') }} </strong>                                     
                        </div>
                    {% endfor %}
                </div>  
                <div class="pagination container">
                    {{ knp_pagination_render(pagination) }}
                </div>


            </div>
        </section>
    </div>
{% endblock %} 
